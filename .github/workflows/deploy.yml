name: Build & Push Docker image

on:
  push:
    branches: ["main"]

permissions:
  contents: read

# Thêm cấu hình này để tắt cảnh báo và chuyển sang dùng Node.js 24
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      # Thiết lập builder tốt hơn cho docker buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 1) Tự động đăng nhập Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: dvnghia
          password: ${{ secrets.DVNGHIA }}

      # 2) Tự động Build image từ Dockerfile và 3) Push lên Docker Hub
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            dvnghia/demo_bai7:latest
            dvnghia/demo_bai7:${{ github.sha }}
                  - name: Triển khai lên DevOps Classroom
        run: |
          REPO="dvnghia/demo_bai7"
          CONTAINER_PORT=80
          RESPONSE=$(curl -sS -w "\n%{http_code}" -X POST "${{ secrets.DEPLOY_URL }}?repo=$REPO&tag=latest&slot=5&container_port=$CONTAINER_PORT&strict_container_port=0")
          BODY=$(echo "$RESPONSE" | sed "$d")
          CODE=$(echo "$RESPONSE" | tail -n1)
          echo "Webhook HTTP: $CODE"
          if [ "$CODE" -lt 200 ] || [ "$CODE" -ge 300 ]; then
            echo "Deploy thất bại. Xem chi tiết trong nhóm log bên dưới."
            echo "::group::Chi tiết webhook JSON"
            echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
            echo "::endgroup::"
            exit 1
          fi
          echo "Deploy thành công."
          echo "::group::Chi tiết webhook JSON"
          echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
          echo "::endgroup::"
